<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Suzan ❤️</title>

<style>
*{
  margin:0;
  padding:0;
  box-sizing:border-box;
  font-family:"Segoe UI",sans-serif;
}

body{
  min-height:100vh;
  background:radial-gradient(circle at top,#2a0a2a,#0b0b12);
  color:white;
  overflow-x:hidden;
  text-align:center;
}

/* ===== INTRO ===== */
.intro{
  position:fixed;
  inset:0;
  background:#0b0b12;
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:1.5rem;
  color:#ff9ecf;
  z-index:1000;
  animation:pulse 2s infinite;
}

@keyframes pulse{
  0%,100%{opacity:.6;}
  50%{opacity:1;}
}

.hidden{display:none;}

/* ===== MAIN CONTENT ===== */
.container{
  padding:30px 15px;
}

h1{
  margin-bottom:15px;
  color:#ff9ecf;
}

.message{
  max-width:400px;
  margin:20px auto;
  line-height:1.6;
  white-space:pre-line;
}

/* ===== FLOATING HEARTS ===== */
.heart{
  position:fixed;
  bottom:-20px;
  animation:rise linear forwards;
}

@keyframes rise{
  to{
    transform:translateY(-110vh);
    opacity:0;
  }
}

/* ===== FIREWORK TEXT ===== */
.firework-text{
  font-size:3rem;
  margin:30px 0;
  opacity:0;
  transition:opacity 1.5s ease;
  text-shadow:
    0 0 10px #ff5fa2,
    0 0 20px #ff5fa2,
    0 0 40px #ff85c2;
}

.firework-text.show{
  opacity:1;
}

/* ===== Romantic Slideshow ===== */
.slideshow{
    position:relative;
    width:90%;
    max-width:400px;
    height:500px;
    margin:40px auto;
    border-radius:20px;
    overflow:hidden;
    box-shadow:0 15px 40px rgba(0,0,0,0.4);
}

.slide{
    position:absolute;
    width:100%;
    height:100%;
    object-fit:cover;
    opacity:0;
    transition:opacity 1.5s ease-in-out, transform 6s ease-in-out;
}

.slide.active{
    opacity:1;
    transform:scale(1.05);
}
</style>
</head>

<body>

<!-- INTRO -->
<div class="intro" id="intro">💌 Tap to Continue 💌</div>

<!-- MUSIC -->
<audio id="bgMusic" loop>
  <source src="valentine.mp3" type="audio/mpeg">
</audio>

<div class="container hidden" id="mainContent">

<h1>Happy Valentine's Day ❤️</h1>

<div class="firework-text" id="fireText">
  SUZAN ❤️
</div>

<p class="message" id="typedMessage"></p>

<!-- Romantic Image Slideshow -->
<div class="slideshow">
    <img src="IMG1.jpg" class="slide active">
    <img src="IMG2.jpg" class="slide">
    <img src="IMG3.jpg" class="slide">
    <img src="IMG4.jpg" class="slide">
    <img src="IMG5.jpg" class="slide">
</div>

</div>

<script>
/* ===== TAP TO START ===== */
const intro=document.getElementById("intro");
const main=document.getElementById("mainContent");
const music=document.getElementById("bgMusic");
const fireText=document.getElementById("fireText");

intro.addEventListener("click",()=>{
  intro.style.display="none";
  main.classList.remove("hidden");
  music.play();
  startTyping();
  startHearts();
  setTimeout(()=>{fireText.classList.add("show");},5000);
},{once:true});

/* ===== Typing Effect ===== */
const text=`Suzan,

From the moment you came into my life,
everything became brighter and warmer.

Your smile is my peace,
your love is my strength.

Forever yours,
— Martin 💕`;

let i=0;
function startTyping(){
  const el=document.getElementById("typedMessage");
  const speed=40;
  const timer=setInterval(()=>{
    el.textContent+=text.charAt(i);
    i++;
    if(i>=text.length) clearInterval(timer);
  },speed);
}

/* ===== Floating Hearts ===== */
function startHearts(){
  setInterval(()=>{
    const heart=document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML="❤️";
    heart.style.left=Math.random()*100+"vw";
    heart.style.fontSize=Math.random()*20+15+"px";
    heart.style.animationDuration=Math.random()*3+3+"s";
    document.body.appendChild(heart);
    setTimeout(()=>{heart.remove()},6000);
  },800);
}

/* ===== Slideshow Script ===== */
const slides=document.querySelectorAll(".slide");
let currentSlide=0;

function nextSlide(){
  slides[currentSlide].classList.remove("active");
  currentSlide=(currentSlide+1)%slides.length;
  slides[currentSlide].classList.add("active");
}

setInterval(nextSlide,4000);
</script>

</body>
</html>
